events {}

http {
  upstream backend {
    server api1:9999;
    server api2:9999;
  }

  server {
    listen 9999;

    location / {
      proxy_pass http://backend;
      proxy_set_header Host $host;
      proxy_http_version 1.1;
      proxy_set_header Connection "";
    }
  }
}
